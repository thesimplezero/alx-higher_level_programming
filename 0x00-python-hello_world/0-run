#!/bin/bash

# Check if the PYFILE variable is set and the corresponding file exists
if [[ -z "$PYFILE" ]]; then
    echo "PYFILE variable is not set."
    exit 1
fi

if [[ ! -f "$PYFILE" ]]; then
    echo "'$PYFILE' does not exist."
    exit 1
fi

# Run the Python script
python3 "$PYFILE"
